<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.huddleup">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.HuddleUp"
        tools:targetApi="31">

        <activity android:name=".N_LauncherActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".N_detailEvent"
            android:exported="false" />

        <activity android:name=".Z_MainActivity"
            android:exported="false" />

        <activity android:name=".N_SigninActivity"
            android:exported="false" />

        <activity android:name=".N_LoginActivity"
            android:exported="false" />

        <activity android:name=".N_EventCheckedInActivity"
            android:exported="false" />

        <activity android:name=".N_EventKu"
            android:exported="false" />


    </application>

</manifest>