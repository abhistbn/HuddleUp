<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.huddleup">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight">

        <!-- Aktivitas utama -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Aktivitas untuk form -->
        <activity android:name=".EventFormActivity" />

        <!-- Nonaktifkan sementara activity yang tidak diperlukan -->
        <!-- <activity android:name=".detailEvent" /> -->
        <!-- <activity android:name=".EventCheckedInActivity" /> -->
        <!-- <activity android:name=".LauncherActivity" /> -->
        <!-- <activity android:name=".LoginActivity" /> -->
        <!-- <activity android:name=".NotificationActivity" /> -->
        <!-- <activity android:name=".SiginActivity" /> -->
        <!-- <activity android:name=".WishlistActivity" /> -->

    </application>

</manifest>
